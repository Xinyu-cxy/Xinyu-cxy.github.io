/* ==============================================
   notes-data.js — AUTO-GENERATED by build-notes.js
   DO NOT EDIT BY HAND. Run: node scripts/build-notes.js
   ============================================== */

const notesData = {
  "Machine Learning": {
    "image": "",
    "notes": {
      "Linear Regression": "<h2>1. Basic Concepts</h2>\n<h3>1.1 Problem Category & Learning Pipeline</h3>\n<p>Supervised Learning $\\rightarrow$ Regression Problem $\\rightarrow$ <strong>Linear Regression</strong></p>\n<p>Learning process:</p>\n$$\\text{training sets} \\xrightarrow{\\text{learning algorithm}} \\text{hypothesis function } h({x})$$\n<h3>1.2 Hypothesis Function</h3>\n<p>Univariate form:</p>\n$$h(x)=\\theta_0+\\theta_1 x$$\n<p>Multivariate form, define $x_0=1$:</p>\n$$h_\\theta({x})=\\sum_{j=0}^n \\theta_j x_j$$\n<p>Vector form:</p>\n$${\\theta}=\n\\begin{bmatrix}\n\\theta_0\\\\\n\\theta_1\\\\\n\\theta_2\n\\end{bmatrix},\\quad\n{x}=\n\\begin{bmatrix}\nx_0\\\\\nx_1\\\\\nx_2\n\\end{bmatrix},\\quad\nh_\\theta({x})={\\theta}^\\mathrm{T}{x}$$\n<p>${\\theta}$: model parameters</p>\n<h3>1.3 Notations</h3>\n<ul><li>$m$: number of training examples (rows in dataset)</li><li>$n$: number of input features</li><li>$x$: input features</li><li>$y$: output target variable</li><li>$(x,y)$: one training example</li><li>$({x}^{(i)},y^{(i)})$: the $i$-th training example, $i=1,2,\\dots,m$</li></ul>\n<p>Goal: Choose parameters ${\\theta}$ such that $h_\\theta({x}^{(i)})\\approx y^{(i)}$ for training data.</p>\n<h2>2. Cost Function: Ordinary Least Squares</h2>\n<p>Linear regression adopts <strong>Ordinary Least Square</strong></p>\n$$J({\\theta})=\\frac{1}{2}\\sum_{i=1}^m \\big(h_\\theta({x}^{(i)})-y^{(i)}\\big)^2$$\n<p>Optimization target:</p>\n$$\\min_{{\\theta}} J({\\theta})$$\n<p>Property: $J({\\theta})$ is a convex function. There is no local optimum other than the global optimum.</p>\n<h2>3. Gradient Descent</h2>\n<h3>3.1 Core Idea</h3>\n<ol><li>Initialize ${\\theta}$ randomly (e.g. ${\\theta}={0}$)</li><li>Iteratively update parameters to reduce $J({\\theta})$</li></ol>\n<p>Update rule:</p>\n$$\\theta_j:=\\theta_j-\\alpha \\frac{\\partial}{\\partial \\theta_j}J({\\theta}),\\quad j=0,1,\\dots,n$$\n<ul><li>$\\alpha$: learning rate, if the learning rate is too large, then you might go past the optimal point and $J({\\theta})$ will increase.</li><li>Partial derivative: defines the steepest descending direction of cost function</li></ul>\n<h3>3.2 Derivation</h3>\n$$\\begin{aligned}\n\\frac{\\partial}{\\partial\\theta_j}J({\\theta})\n&=\\frac{\\partial}{\\partial\\theta_j}\\left[\\frac12\\big(h_\\theta({x})-y\\big)^2\\right]\\\\\n&=\\big(h_\\theta({x})-y\\big)\\cdot\\frac{\\partial}{\\partial\\theta_j}h_\\theta({x})\\\\\n&=x_j\\cdot\\big(h_\\theta({x})-y\\big)\n\\end{aligned}$$\n<h3>3.3 Batch Gradient Descent</h3>\n$$\\theta_j:=\\theta_j-\\alpha\\sum_{i=1}^m x_j^{(i)}\\big(h_\\theta({x}^{(i)})-y^{(i)}\\big)$$\n<p>Batch means every parameter update requires traversing all $m$ training samples.</p>\n<h3>3.4 Stochastic Gradient Descent</h3>\n<p>Update parameters sample by sample, without using the whole dataset in one iteration:</p>\n$$\\begin{aligned}\n&\\text{Repeat } \\{ \\\\\n&\\quad \\text{for } i = 1 \\text{ to } m \\{ \\\\\n&\\qquad \\theta_j := \\theta_j - \\alpha x_j^{(i)} \\big(h_\\theta(x^{(i)}) - y^{(i)}\\big) \\\\\n&\\quad \\} \\\\\n&\\}\n\\end{aligned}$$\n<h2>4. Normal Equation (Analytical Solution)</h2>\n<p>Gradient descent is an iterative numerical method. The normal equation computes the optimal parameter directly with closed-form solution.</p>\n<h3>4.1 Matrix Gradient & Trace</h3>\n<p>Let $f(A)$ be a function mapping matrix to real scalar. Matrix gradient definition:</p>\n$$\\nabla_A f(A)=\n\\begin{bmatrix}\n\\frac{\\partial f}{\\partial A_{11}} & \\dots & \\frac{\\partial f}{\\partial A_{1n}}\\\\\n\\vdots & \\ddots & \\vdots\\\\\n\\frac{\\partial f}{\\partial A_{n1}} & \\dots & \\frac{\\partial f}{\\partial A_{nn}}\n\\end{bmatrix}$$\n<p>Trace of square matrix:</p>\n$$\\mathrm{tr}(A)=\\sum_i A_{ii}$$\n<p>Properties of trace:</p>\n<ul><li>$\\mathrm{tr}(A)=\\mathrm{tr}(A^\\mathrm{T})$</li><li>$\\mathrm{tr}(AB)=\\mathrm{tr}(BA)$</li><li>$\\mathrm{tr}(ABC)=\\mathrm{tr}(BCA)$</li></ul>\n<p>Properties of matix gradient:</p>\n<ul><li>$\\nabla_{\\theta}\\big({\\theta}^\\mathrm{T}{b}\\big)={b}$</li><li>$\\nabla_{\\theta}\\big({\\theta}^\\mathrm{T}A{\\theta}\\big)=(A+A^\\mathrm{T}){\\theta}$</li></ul>\n<h3>4.2 Matrix Form of Cost Function</h3>\n<p>Design matrix ${X}\\in\\mathbb{R}^{m\\times(n+1)}$, label vector ${y}\\in\\mathbb{R}^m$:</p>\n$${X}=\n\\begin{bmatrix}\n({x}^{(1)})^\\mathrm{T}\\\\\n({x}^{(2)})^\\mathrm{T}\\\\\n\\vdots\\\\\n({x}^{(m)})^\\mathrm{T}\n\\end{bmatrix},\\quad\n{y}=\n\\begin{bmatrix}\ny^{(1)}\\\\\ny^{(2)}\\\\\n\\vdots\\\\\ny^{(m)}\n\\end{bmatrix}$$\n<p>Cost function:</p>\n$$J({\\theta})=\\frac12({X}{\\theta}-{y})^\\mathrm{T}({X}{\\theta}-{y})$$\n<h3>4.3 Derivation of Normal Equation</h3>\n<p>Optimality condition: gradient equals zero vector $\\nabla_{\\theta}J({\\theta})={0}$</p>\n$$\\begin{aligned}\n\\nabla_{\\theta}J({\\theta})\n&=\\frac12\\nabla_{\\theta}\\big(({X}{\\theta}-{y})^\\mathrm{T}({X}{\\theta}-{y})\\big)\\\\\n&={X}^\\mathrm{T}{X}{\\theta}-{X}^\\mathrm{T}{y}={0}\n\\end{aligned}$$\n<p>Normal equation:</p>\n$${X}^\\mathrm{T}{X}{\\theta}={X}^\\mathrm{T}{y}$$\n<p>Closed-form solution for optimal parameters:</p>\n$${\\theta}=({X}^\\mathrm{T}{X})^{-1}{X}^\\mathrm{T}{y}$$\n<p></p>",
      "Locally Weighted Regression and Logistic Regression": "<h2>1. Locally Weighted Regression</h2>\n<p>Locally weighted regression is an approach to modify linear regression so that it can fit non-linear functions.</p>\n<h3>1.1 Parametric and Non-parametric Learning Algorithms</h3>\n<ul><li><strong>Parametric learning algorithm</strong>: Has a fixed set of parameters $\\theta_0,\\dots,\\theta_n$ to learn.</li><li><strong>Non-parametric learning algorithm</strong>: The amount of data/parameters grows linearly with the size of training data.</li></ul>\n<p>Locally weighted regression belongs to non-parametric algorithms. We refit parameters for every prediction point, rather than learning a fixed global set of parameters once.</p>\n<h3>1.2 Core Idea</h3>\n<p>To evaluate $h(x)$ at a target point $x$: Fit a straight line using only data points close to $x$ on the horizontal axis. We assign higher weights to samples nearby.</p>\n<h3>1.3 Weighted Cost Function</h3>\n<p>Fit parameter vector $\\theta$ to minimize:</p>\n$$\\sum_{i=1}^m w^{(i)}\\big(y^{(i)} - \\theta^\\mathrm{T}{x}^{(i)}\\big)^2$$\n<p>Where $w^{(i)}$ denotes the weight function.</p>\n<p>Default Gaussian weight function:</p>\n$$w^{(i)}=\\exp\\left(-\\frac{\\big(x^{(i)}-x\\big)^2}{2\\tau^2}\\right)$$\n<ul><li>$x$: the location where we want to make prediction</li><li>$\\tau$: bandwidth parameter, controls effective range of weights, affects overfitting / underfitting.</li></ul>\n<p>Property: If $|x^{(i)}-x|$ is small, $w^{(i)}\\approx1$; If $|x^{(i)}-x|$ is large, $w^{(i)}\\approx0$.</p>\n<h2>2. Probabilistic Interpretation of Linear Regression</h2>\n<h3>2.1 Assumptions</h3>\n<p>Assume:</p>\n$$y^{(i)}={\\theta}^\\mathrm{T}{x}^{(i)}+\\varepsilon^{(i)}$$\n<p>$\\varepsilon^{(i)}$: random noise / unmodeled effects.</p>\n<p>We further assume:</p>\n$$\\varepsilon^{(i)} \\sim \\mathcal{N}(0,\\sigma^2)$$\n<p>Probability density of noise:</p>\n$$p(\\varepsilon^{(i)})=\\frac{1}{\\sqrt{2\\pi}\\sigma}\\exp\\left(-\\frac{(\\varepsilon^{(i)})^2}{2\\sigma^2}\\right)$$\n<p>Assume all $\\varepsilon^{(i)}$ are independent and identically distributed (i.i.d.).</p>\n<p>Equivalently:</p>\n$$y^{(i)}\\mid {x}^{(i)};{\\theta} \\sim \\mathcal{N}\\big({\\theta}^\\mathrm{T}{x}^{(i)},\\sigma^2\\big)$$\n<h3>2.2 Likelihood Function</h3>\n<p>Likelihood of parameter ${\\theta}$:</p>\n$$L({\\theta})=p(\\vec{y}\\mid X;{\\theta})=\\prod_{i=1}^m p\\big(y^{(i)}\\mid {x}^{(i)};{\\theta}\\big)$$\n$$L({\\theta})=\\prod_{i=1}^m \\frac{1}{\\sqrt{2\\pi}\\sigma}\\exp\\left(-\\frac{\\big(y^{(i)}-{\\theta}^\\mathrm{T}{x}^{(i)}\\big)^2}{2\\sigma^2}\\right)$$\n<h4>Likelihood and Probability</h4>\n<ul><li><strong>Probability</strong>: Treat ${\\theta}$ as fixed constant, function of data.</li><li><strong>Likelihood</strong>: Treat training data as fixed, function of parameter ${\\theta}$.</li></ul>\n<h3>2.3 Log-Likelihood</h3>\n$$\\ell({\\theta})=\\log L({\\theta})$$\n<h3>2.4 Maximum Likelihood Estimation (MLE)</h3>\n<p>Choose ${\\theta}$ to maximize $\\ell({\\theta})$. This is equivalent to minimizing:</p>\n$$\\frac12\\sum_{i=1}^m\\big(y^{(i)}-{\\theta}^\\mathrm{T}{x}^{(i)}\\big)^2 = J({\\theta})$$\n<p>This explains why we adopt squared error cost function in linear regression.</p>\n<h2>3. Classification Problem and Logistic Regression</h2>\n<p>Binary classification: target variable $y\\in\\{0,1\\}$. Linear regression is unsuitable for classification tasks.</p>\n<h3>3.1 Logistic Function</h3>\n<p>We require hypothesis output $h_\\theta(x)\\in[0,1]$ to represent probability.</p>\n$$h_\\theta({x})=g\\big({\\theta}^\\mathrm{T}{x}\\big),\\quad\ng(z)=\\frac{1}{1+e^{-z}}$$\n<p>$g(z)$ maps any real number into range $(0,1)$.</p>\n<h3>3.2 Probability Assumption</h3>\n<p>Assume:</p>\n$$\\begin{cases}\nP(y=1\\mid {x};{\\theta})=h_\\theta({x})\\\\\nP(y=0\\mid {x};{\\theta})=1-h_\\theta({x})\n\\end{cases}$$\n<p>Compact form:</p>\n$$P(y\\mid {x};{\\theta})=\\big(h_\\theta({x})\\big)^y \\big(1-h_\\theta({x})\\big)^{1-y}$$\n<h3>3.3 Likelihood & Log-Likelihood</h3>\n$$L({\\theta})=p(\\vec{y}\\mid X;{\\theta})=\\prod_{i=1}^m \\big(h_\\theta({x}^{(i)})\\big)^{y^{(i)}} \\big(1-h_\\theta({x}^{(i)})\\big)^{1-y^{(i)}}$$\n<p>Log-likelihood:</p>\n$$\\ell({\\theta})=\\sum_{i=1}^m \\Big[y^{(i)}\\log h_\\theta({x}^{(i)})+\\big(1-y^{(i)}\\big)\\log\\big(1-h_\\theta({x}^{(i)})\\big)\\Big]$$\n<p>Our goal: choose ${\\theta}$ to maximize $\\ell({\\theta})$.</p>\n<h3>3.4 Batch Gradient Ascent</h3>\n<p>Update rule for gradient ascent (maximization):</p>\n$$\\theta_j:=\\theta_j+\\alpha \\frac{\\partial}{\\partial\\theta_j}\\ell({\\theta})$$\n<p>Final update formula:</p>\n$$\\theta_j:=\\theta_j+\\alpha\\sum_{i=1}^m \\big(y^{(i)}-h_\\theta({x}^{(i)})\\big)x_j^{(i)}$$\n<p>> Note: Although the update expression has identical form to linear regression gradient descent, $h_\\theta({x})$ is defined differently.</p>\n<h2>4. Newton's Method</h2>\n<p>Gradient ascent takes small incremental steps and requires many iterations. Newton's method makes larger jumps and converges in fewer iterations.</p>\n<h3>4.1 One-dimensional Case</h3>\n<p>Goal: Find $\\theta$ such that $f(\\theta)=0$. When maximizing $\\ell({\\theta})$, we solve $\\ell'(\\theta)=0$.</p>\n<p>Iteration rule:</p>\n$$\\theta^{(t+1)}=\\theta^{(t)}-\\frac{f(\\theta^{(t)})}{f'(\\theta^{(t)})}$$\n<h3>4.2 Vector-valued Generalization</h3>\n<p>For vector parameter ${\\theta}\\in\\mathbb{R}^{n+1}$:</p>\n$${\\theta}^{(t+1)}={\\theta}^{(t)}-{H}^{-1}\\nabla\\ell$$\n<ul><li>$\\nabla\\ell$: gradient vector of log-likelihood</li><li>${H}$: Hessian matrix, $H_{ij}=\\dfrac{\\partial^2 \\ell}{\\partial\\theta_i\\partial\\theta_j}$</li></ul>\n<h3>4.3 Quadratic Convergence</h3>\n<p>Newton's method enjoys quadratic convergence. When the current estimate is close to optimal value, the error shrinks extremely rapidly in each iteration.</p>\n<p>Trade-off: Each iteration requires computing and inverting the Hessian matrix, which carries heavy computational cost.</p>",
      "Perceptron, GLM and Softmax Regression": "<h2>1. Perceptron Algorithm</h2>\n<p>Perceptron uses sign function:</p>\n$$g(z)=\r\n\\begin{cases}\r\n1 & z \\ge 0 \\\\\r\n0 & z < 0\r\n\\end{cases}$$\n<p>Model:</p>\n$$h_\\theta(x)=g\\left(\\theta^\\top x\\right)$$\n<p>Update rule (similar to logistic and linear regression, different hypothesis):</p>\n$$\\theta_j' = \\theta_j + \\alpha \\sum_{i=1}^m \\big(y^{(i)} - h_\\theta(x^{(i)})\\big)\\,x_j^{(i)}$$\n<p>$y^{(i)} - h_\\theta(x^{(i)})$:</p>\n<ul><li>= 0 if predicted correctly</li><li>= -1 if $y^{(i)}$ = 0 and wrong</li><li>= 1 if $y^{(i)}$ = 1 and wrong</li></ul>\n<p>For a new misclassified example:</p>\n$$\\begin{aligned}\r\n\\theta_j'\r\n&= \\theta_j + \\alpha\\big(y^{(i)}-h_\\theta(x^{(i)})\\big)x_j^{(i)}\\\\\r\n&= \\theta_j \\pm \\alpha x_j^{i}\r\n\\end{aligned}$$\n<h2>2. Exponential Family</h2>\n<p>PDF (probability density function) general form of exponential family distribution:</p>\n$$p(y;\\eta)=b(y)\\exp\\big[\\eta^\\top T(y)-a(\\eta)\\big]$$\n<p>Notation:</p>\n<ul><li>$y$: data</li><li>$\\eta$: natural parameter</li><li>$T(y)$: sufficient statistic, here $T(y)=y$</li><li>$b(y)$: base measure</li><li>$a(\\eta)$: log‑partition function (normalizer)</li></ul>\n<h3>2.1 Bernoulli (binary data)</h3>\n<p>Bernoulli PDF:</p>\n$$p(y;\\phi)=\\phi^y(1-\\phi)^{(1-y)}$$\n<p>Rewrite to exponential family form:</p>\n$$\\begin{aligned}\r\np(y;\\phi)\r\n&=\\exp\\Big(\\log\\big(\\phi^y(1-\\phi)^{1-y}\\big)\\Big) \\\\\r\n&=\\exp\\Big(y\\log\\frac{\\phi}{1-\\phi}+\\log(1-\\phi)\\Big)\r\n\\end{aligned}$$\n<p>Identify components:</p>\n<ul><li>$b(y)=1$</li><li>$T(y)=y$</li><li>Natural parameter(scalar $\\eta^\\top = \\eta$):</li></ul>\n$$\\eta=\\log\\frac{\\phi}{1-\\phi} \\quad\\Rightarrow\\quad \\phi=\\frac{1}{1+e^{-\\eta}}$$\n<ul><li>Log‑partition:</li></ul>\n$$a(\\eta)=-\\log(1-\\phi)$$\n<h3>2.2 Gaussian (fixed variance $\\sigma^2=1$)</h3>\n<p>Gaussian PDF:</p>\n$$p(y;\\mu)=\\frac{1}{\\sqrt{2\\pi}}\\exp\\left(-\\frac{(y-\\mu)^2}{2}\\right)$$\n<p>Rewrite:</p>\n$$p(y;\\mu)=\\frac{1}{\\sqrt{2\\pi}}e^{-\\frac{y^2}{2}}\\exp\\left(\\mu y-\\frac12\\mu^2\\right)$$\n<p>Components:</p>\n<ul><li>$b(y)=\\frac{1}{\\sqrt{2\\pi}}e^{-\\frac{y^2}{2}}$</li><li>$T(y)=y$</li><li>$a(\\eta)=\\frac12\\mu^2$</li><li>$\\eta=\\mu$</li></ul>\n<p>> Bernoulli and Gaussian both belong to exponential family.</p>\n<h3>2.3 Properties for exponential family w.r.t natural parameter $\\eta$</h3>\n<p>a) Maximum‑likelihood estimation objective is <strong>concave</strong> in $\\eta$; negative log‑likelihood is convex.</p>\n<p>b) Mean property:</p>\n$$\\mathbb{E}[y;\\eta]=\\frac{\\partial}{\\partial\\eta}a(\\eta)$$\n<p>c) Variance property:</p>\n$$\\mathrm{Var}(y;\\eta)=\\frac{\\partial^2}{\\partial\\eta^2}a(\\eta)$$\n<h2>3. Generalized Linear Model (GLM)</h2>\n<h3>Assumptions / design choices</h3>\n<p>a) Given $x$, $y$ comes from an exponential‑family distribution: $y|x;\\theta \\sim \\mathrm{ExponentialFamily}(\\eta)$.</p>\n<p>Common choices:</p>\n<ul><li>Real‑valued data → Gaussian</li><li>Binary data → Bernoulli</li><li>Count (non‑negative integer) → Poisson</li><li>Positive real $\\mathbb R^+$ → Gamma / Exponential</li><li>Other distributions: Beta, Dirichlet</li></ul>\n<p>b) Natural parameter is linear in input:</p>\n$$\\eta=\\theta^\\top x,\\quad \\theta\\in\\mathbb R^n,\\;x\\in\\mathbb R^n$$\n<p>c) Prediction output expected value:</p>\n$$h_\\theta(x)=\\mathbb E\\big[y\\mid x;\\theta\\big]$$\n<p>Learning objective: maximize log‑likelihood</p>\n$$\\max_\\theta \\sum_{i}\\log p\\big(y^{(i)};\\theta^\\top x^{(i)}\\big)$$\n<h3>4. GLM Training</h3>\n<p>Batch gradient ascent update rule:</p>\n$$\\theta_j' := \\theta_j + \\alpha \\sum_{i}\\big(y^{(i)}-h_\\theta(x^{(i)})\\big)\\,x_j^{(i)}$$\n<p>Terminology:</p>\n<ul><li>$\\eta$: natural parameter</li><li>Canonical response function: $\\mu = g(\\eta)=\\frac{\\partial}{\\partial\\eta}a(\\eta) = \\mathbb E[y;\\eta]$</li><li>Canonical link function: $\\eta = g^{-1}(\\mu)$</li></ul>\n<p>Three parametrization:</p>\n$$\\underbrace{\\theta^\\top x}_{\\text{model param}}\r\n\\;\\rightarrow\\;\r\n\\underbrace{\\eta}_{\\text{natural param}}\r\n\\;\\xrightarrow{g}\\;\r\n\\underbrace{\\phi/\\mu/\\lambda}_{\\text{canonical param}}$$\n<ul><li>Bernoulli: $\\phi$</li><li>Gaussian: $\\mu$</li><li>Poisson: $\\lambda$</li></ul>\n<h4>Example 1: Logistic Regression</h4>\n$$h_\\theta(x)=\\mathbb E[y|x;\\theta]=\\phi=\\frac{1}{1+e^{-\\theta^\\top x}}$$\n<p>Update:</p>\n$$\\theta_j' = \\theta_j + \\alpha \\sum_i \\big(y^{(i)}-h_\\theta(x^{(i)})\\big)\\,x_j^{(i)}$$\n<h4>Example 2: Linear Regression</h4>\n<p>$y|x;\\theta \\sim \\mathrm{Gaussian}$,</p>\n$$\\eta=\\mu=\\theta^\\top x$$\n<h2>5. Softmax Regression</h2>\n<p>Multi‑class classification, minimize cross‑entropy.</p>\n<ul><li>$K$: number of classes</li><li>Input $x^{(i)}\\in\\mathbb R^n$</li><li>Label $y$: one‑hot vector $\\in\\{0,1\\}^K$, e.g. $[0,0,1]$</li><li>Parameter matrix: $K$ weight vectors $\\theta_\\mathit{class}\\in\\mathbb R^n$</li></ul>\n<p>Softmax probability:</p>\n$$p(y=k\\mid x;\\theta)=\\frac{\\exp(\\theta_k^\\top x)}{\\sum_{l=1}^K\\exp(\\theta_l^\\top x)}$$\n<p>Cross‑entropy loss between true distribution $p$ and predicted $\\hat p$:</p>\n$$\\mathrm{cross\\_entropy}(p,\\hat p)\r\n=-\\sum_{y\\in\\mathit{classes}} p(y)\\log \\hat p(y)$$\n<p>For one‑hot label $y_0$ (only one entry is 1):</p>\n$$\\mathcal L = -\\log\\;\\hat p(y_0)\r\n= -\\log\\left(\r\n\\frac{e^{\\theta_{y_0}^\\top x}}\r\n{\\sum_{i\\in\\mathit{classes}} e^{\\theta_i^\\top x}}\r\n\\right)$$\n<p>Treat cross‑entropy as loss function and optimize with gradient descent.</p>",
      "Regularization Techniques": "<p>Regularization prevents overfitting by constraining model complexity. Common approaches:</p>\n<ul><li><strong>L1/L2 regularization</strong> — add a penalty on parameter magnitude to the loss</li><li><strong>Dropout</strong> — randomly zero out neurons during training</li><li><strong>Data augmentation</strong> — generate synthetic training examples</li><li><strong>Early stopping</strong> — halt training when validation error stops improving</li></ul>",
      "Understanding Gradient Descent": "<p>Gradient descent is the backbone of modern machine learning optimization. It iteratively moves parameters in the direction that minimizes the loss function.</p>\n<p>Variants include:</p>\n<ul><li><strong>SGD</strong> — uses a single or mini-batch of examples per step</li><li><strong>Adam</strong> — adaptive learning rates with momentum</li><li><strong>AdamW</strong> — decouples weight decay from the gradient update</li></ul>"
    }
  },
  "Programming Languages": {
    "image": "",
    "notes": {
      "Python Async Deep Dive": "<p>Python's <code>async/await</code> syntax enables cooperative concurrency. The event loop runs in a single thread, switching between tasks at <code>await</code> points.</p>\n<p>This model shines for I/O-bound workloads where tasks spend most of their time waiting on network or disk. For CPU-bound work, consider <code>concurrent.futures</code> or multiprocessing instead. </p>",
      "Rust Ownership Model": "<p>Rust's ownership system is its most distinctive feature. The three core rules:</p>\n<ul><li>Each value has exactly one <strong>owner</strong></li><li>There can be either one mutable reference <em>or</em> any number of immutable references, but never both simultaneously</li><li>References must always be valid</li></ul>\n<p>These rules are enforced at compile time, eliminating entire categories of bugs without a garbage collector. </p>"
    }
  },
  "Systems & Infrastructure": {
    "image": "",
    "notes": {
      "Building Scalable APIs": "<p>Designing APIs that handle growth requires careful consideration of statelessness, caching strategies, and database optimization.</p>\n<p>Key principles:</p>\n<ul><li>Keep services stateless — store session data in shared caches like Redis</li><li>Use connection pooling for database access</li><li>Implement rate limiting and circuit breakers for resilience</li><li>Design for horizontal scaling from day one</li></ul>",
      "Container Orchestration Basics": "<p>Container orchestration platforms like Kubernetes automate deployment, scaling, and management of containerized applications.</p>\n<p>Core concepts include Pods (the smallest deployable unit), Services (stable network endpoints), and Deployments (declarative update management). </p>"
    }
  }
};
